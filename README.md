# Adaptive-Layout-Engine-for-Multi-Surface-Ads
A pure, testable layout engine that adapts a single ad's content across multiple ad surface sizes in real time, with an optional AI layer for generating ad copy.
